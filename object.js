/**

 */
const nesh = {
    name: "Neshad",
    age: 27,
    relation: "single",
}

// const name = nesh.age;
// const key = Object.keys(nesh);
// const value = Object.values(nesh);
console.log(name);